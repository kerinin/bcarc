<% content_for :head do %>
  <%= stylesheet_link_tag 'header', :cache => true %>
  <%= stylesheet_link_tag 'page', :cache => true %>
  <%= stylesheet_link_tag 'footer', :cache => true %>
  
	<title><%= @page.name %> @ Bercy Chen Studio: <%= t('architecture') %> + <%= t('construction') %></title>
	<meta name="keywords" content="<%= @page.name %>"/>
<% end %>

<div class="content">
	<h1><%= @page.name %></h1>
	<%= @page.html_content %>
</div>

